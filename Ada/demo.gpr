project Demo is

    for Languages use ("Ada", "C");

    for Source_Dirs use ("src");

    for Main use 
    (
        "hello_world.adb",
        "main.adb",
        "test_call_swap.c"
    );

    for Object_Dir use "obj";

    for Exec_Dir use "bin";

end Demo;
