library project HelloLib is

    for Source_Dirs use ("src");

    for Excluded_Source_Files use 
    (
        "main.cpp"
    );

    for Object_Dir use "obj4lib";

    for Languages use ("C++");

    for Library_Dir use "lib";
    for Library_Name use "hello";
    for Library_Kind use "dynamic";

end HelloLib;
